class Solution {
public:

    vector<string>v;

    string encode(vector<string>& strs) {
        string ans="";
        for(auto s:strs){
            v.push_back(s);
        }
        return ans;
    }

    vector<string> decode(string s) {
        vector<string> ans;
        
        return v;
    }
};
